# 1. Observe o código a seguir e, em seguida, responda: 
x = [] 

# for in in range(10):
#     a = int(input())
#     x.append(a) 

print(min(x))
print(max(x)) 

# a) O código está sintaticamente correto?
# Não. O código apresenta um erro de sintaxe na linha 4, onde a palavra reservada 'in' foi utilizada como nome de variável, o que não é permitido.  
# b) Em linhas gerais, o que realiza este código?
# Realiza a leitura de 10 números inteiros e armazena-os em uma lista. Em seguida, imprime o menor e o maior valor da lista. 